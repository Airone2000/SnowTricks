{% extends 'base.html.twig' %}

{% block body %}
    <h1>Family</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ family.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ family.name }}</td>
            </tr>
            <tr>
                <th>Introduction</th>
                <td>{{ family.introduction }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('figures_familles_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('figures_familles_edit', { 'id': family.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
